# LLM 提供商: mock（默认，无需 API）| qwen（通义千问）
LLM_PROVIDER=mock

# 通义千问（仅当 LLM_PROVIDER=qwen 时需要）
# 在 https://dashscope.console.aliyun.com/ 创建 API Key
DASHSCOPE_API_KEY=
# 可选，默认 qwen-plus
QWEN_MODEL=qwen-plus
